Nestjs Oauth Social Login example/template with jwt, docker full support and postgres and typeorm
to run project just provide appropriate env variables and run 
$ docker compose up

at the root of the project